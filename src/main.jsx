import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";
import 'antd/dist/reset.css'; // Ant Designのリセットスタイルを読み込む

ReactDOM.createRoot(document.getElementById("root")).render(
    <App />
);